<section class="section collection-list reveal{% if section.settings.color_scheme == 'alt' %} bg-alt{% endif %}">
  <div class="page-width">
    {%- if section.settings.title != blank -%}
      <div class="section-heading">
        {%- if section.settings.eyebrow != blank -%}<p class="eyebrow">{{ section.settings.eyebrow | escape }}</p>{%- endif -%}
        <h2 class="section-heading__title">{{ section.settings.title | escape }}</h2>
      </div>
    {%- endif -%}

    {%- assign layout_class = '' -%}
    {%- if section.settings.layout == 'bento' -%}{%- assign layout_class = ' collection-list__grid--bento' -%}{%- endif -%}
    <div class="collection-list__grid stagger{{ layout_class }}">
      {%- liquid
        assign blocks_have_collections = false
        for block in section.blocks
          assign test_coll = collections[block.settings.collection]
          if test_coll != blank
            assign blocks_have_collections = true
            break
          endif
        endfor
      -%}

      {%- if section.blocks.size > 0 and blocks_have_collections -%}
        {%- for block in section.blocks -%}
          {%- assign coll = collections[block.settings.collection] -%}
          {%- if coll != blank -%}
            <a href="{{ coll.url }}" class="collection-card" {{ block.shopify_attributes }}>
              {%- if coll.featured_image -%}
                {{ coll.featured_image | image_url: width: 1600 | image_tag: loading: 'lazy', alt: coll.title | escape }}
              {%- else -%}
                {{ 'collection-' | append: forloop.index | placeholder_svg_tag: 'collection-card__placeholder' }}
              {%- endif -%}
              <div class="collection-card__overlay">
                <div class="collection-card__top">
                  <span class="collection-card__count">{{ coll.products_count }} Products</span>
                </div>
                <div class="collection-card__bottom">
                  <h3 class="collection-card__title">{{ coll.title | escape }}</h3>
                  <span class="collection-card__cta">
                    Explore
                    <svg viewBox="0 0 24 24"><path d="M5 12h14M13 5l7 7-7 7" stroke-linecap="round" stroke-linejoin="round"/></svg>
                  </span>
                </div>
              </div>
            </a>
          {%- endif -%}
        {%- endfor -%}
      {%- else -%}
        {%- assign counter = 0 -%}
        {%- for coll in collections -%}
          {%- assign counter = counter | plus: 1 -%}
          {%- if counter > 3 -%}{%- break -%}{%- endif -%}
          <a href="{{ coll.url }}" class="collection-card">
            {%- if coll.featured_image -%}
              {{ coll.featured_image | image_url: width: 1600 | image_tag: loading: 'lazy', alt: coll.title | escape }}
            {%- else -%}
              {{ 'collection-' | append: counter | placeholder_svg_tag: 'collection-card__placeholder' }}
            {%- endif -%}
            <div class="collection-card__overlay">
              <div class="collection-card__top">
                <span class="collection-card__count">{{ coll.products_count }} Products</span>
              </div>
              <div class="collection-card__bottom">
                <h3 class="collection-card__title">{{ coll.title | escape }}</h3>
                <span class="collection-card__cta">
                  Explore
                  <svg viewBox="0 0 24 24"><path d="M5 12h14M13 5l7 7-7 7" stroke-linecap="round" stroke-linejoin="round"/></svg>
                </span>
              </div>
            </div>
          </a>
        {%- endfor -%}
      {%- endif -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Collection list",
  "tag": "section",
  "settings": [
    { "type": "select", "id": "color_scheme", "label": "Color scheme",
      "options": [ { "value": "default", "label": "Default" }, { "value": "alt", "label": "Alt background" } ], "default": "default" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow" },
    { "type": "text", "id": "title", "label": "Heading", "default": "Shop by category" },
    { "type": "select", "id": "layout", "label": "Layout",
      "options": [ { "value": "grid", "label": "Uniform grid" }, { "value": "bento", "label": "Bento (compact asymmetric)" } ], "default": "bento" },
    { "type": "range", "id": "columns", "min": 2, "max": 4, "step": 1, "label": "Columns (grid mode)", "default": 3 }
  ],
  "max_blocks": 8,
  "blocks": [
    {
      "type": "collection",
      "name": "Collection",
      "settings": [ { "type": "collection", "id": "collection", "label": "Collection" } ]
    }
  ],
  "presets": [
    {
      "name": "Collection list",
      "blocks": [ { "type": "collection" }, { "type": "collection" }, { "type": "collection" }, { "type": "collection" } ]
    }
  ]
}
{% endschema %}